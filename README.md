# Game project

To run the game. Execute the following commands
```sh
cd game
python3 main.py
 ```

# App project

```sh
git clone
cd app
python3 -m venv env
source env/bin/activate
pip3 install -r requirements
```